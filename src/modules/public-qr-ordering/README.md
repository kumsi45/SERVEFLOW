# Public QR Ordering

Phase 3 anonymous public QR ordering module.

Step 1 adds isolated cart state only. It is not wired into routing, checkout, or
Supabase calls.

This module must not introduce authentication, admin behavior, or direct order
submission logic.
