const fs=require("fs"),path=require("path");const root=path.resolve(__dirname,"../..");const read=p=>fs.readFileSync(path.join(root,p),"utf8");const router=read("src/app/router/AppRouter.tsx"),gateway=read("src/app/router/RoleNamespaceRoute.tsx"),page=read("src/modules/waiter-dashboard/pages/WaiterDashboardPage.tsx"),service=read("src/modules/waiter-dashboard/services/waiterDashboardService.ts"),migration=read("supabase/migrations/071_waiter_dashboard_table_management_phasew2.sql");const checks=[];const check=(l,o)=>checks.push({l,o:Boolean(o)});check("Canonical waiter dashboard route exists",router.includes('waiter: ["dashboard"')&&gateway.includes("WaiterDashboardPage"));check("Dashboard loads canonical table read model",page.includes("loadWaiterDashboardTables")&&service.includes("get_waiter_dashboard_tables"));check("Table, assignment and order realtime remain",["restaurant_tables","restaurant_table_waiter_assignments","orders"].every(t=>page.includes(t)));check("Existing W2 assignment model remains",migration.includes("restaurant_table_waiter_assignments")&&migration.includes("get_waiter_dashboard_tables"));check("Production UI is table-first",page.includes('useState<ProductivityView>("tables")')&&page.includes("w10-grid"));for(const c of checks)console.log(`${c.o?"PASS":"FAIL"} ${c.l}`);if(checks.some(c=>!c.o))process.exit(1);console.log("\nPASS");
